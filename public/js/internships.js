// Reuses jobs.js logic, we can keep this file empty or just load jobs.js
// but as per requirements, writing a specific file
document.addEventListener('DOMContentLoaded', () => {
  // Logic handled in jobs.js by checking pathname for 'internships.html'
  // But just to be complete and ensure functionality if scripts are separated:
});
